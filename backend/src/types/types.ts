/* eslint-disable camelcase */
/* using camel case for url variables */

export type ReqQueryDate = {
    from_date?: string,
    to_date?: string,
};

export type ReqQuerySearch = {
    name: string
};
